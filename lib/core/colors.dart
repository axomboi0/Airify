// lib/core/colors.dart

import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0B0B0F);
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;

  static const Color glassLight = Color.fromARGB(70, 255, 255, 255);
  static const Color glassBorder = Color.fromARGB(40, 255, 255, 255);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFBDBDBD);

  // Buttons & accents
  static const Color blueAccent = Color(0xFF3797EF);
  static const Color redAccent = Color(0xFFFF4C4C);
}
